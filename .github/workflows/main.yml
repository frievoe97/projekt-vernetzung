# name: Upload the Website to the server of the domain projekt-vernetzung.friedrichvoelkers.de

# on:
#   push:
#     branches: [main]
#   schedule:
#     - cron: '40 14 * * *'  # Jede Nacht um Mitternacht UTC

# jobs:
#   Upload-to-SFTP:
#     runs-on: ubuntu-latest
# 
#     steps:
#       - name: 🚚 Get latest code
#         uses: actions/checkout@v3
# 
#       - uses: actions/setup-node@v3
#         with:
#           node-version: 18
#       - run: npm install
#       - run: npm run build
# 
#       - name: 📂 SFTP uploader
#         uses: wangyucode/sftp-upload-action@v1.4.8 # Danke an @https://github.com/wangyucode/sftp-upload-action
#         with:
#           host: ${{ secrets.SFTP_HOST }}
#           username: ${{ secrets.SFTP_USERNAME }}
#           password: ${{ secrets.SFTP_PASSWORD }}
#           dryRun: false
#           forceUpload: true
#           localDir: "./dist"
#           remoteDir: "/projekt-vernetzung"
#           exclude: ".git*,.DS_Store"
